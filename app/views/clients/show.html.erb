<%= notice %>

<div class="card">
  <div class="card-body">
    <h2 class="card-title">👤 <%= @client.name %></h2>
    <p class="card-text">📧 <%= @client.email %></p>
    <a href="#" class="btn btn-primary mt-1">Send monthly report</a>
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item"><%= link_to "Back to clients", clients_path%></li>
    <li class="list-group-item"><%= link_to "Edit this client", edit_client_path(@client) %></li>
    <li class="list-group-item"><%= link_to "Delete this client", @client, method: :delete %></li>
  </ul>
</div>
